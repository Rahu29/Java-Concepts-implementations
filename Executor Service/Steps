1) Create new instance of executorService and initialize it with "FixedThreadPool" which is present inside Executors, and pass number of threads as per the requirement.
2) Run a for loop and for each iteration write a task inside "submit()" functional interface.
3) After completing all the task shutdown the executor service.